spring:
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: infra-config-server
  zipkin:
    discovery:
      enabled: true
      serviceId: infra-zipkin

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka