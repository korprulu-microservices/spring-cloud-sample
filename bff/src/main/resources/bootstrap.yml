spring:
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: infra-config-server

feign:
  hystrix:
    enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka