spring:
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: infra-config-server
  zipkin:
    discovery:
      enabled: true
      serviceId: infra-zipkin

feign:
  hystrix:
    enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka